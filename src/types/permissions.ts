// Central export for the PermissionKey union type.
// The source of truth lives in src/permissions.ts alongside PERMISSION_KEYS.
// Import from here when you need the type without importing the full module.
export type { PermissionKey } from "@/permissions";
